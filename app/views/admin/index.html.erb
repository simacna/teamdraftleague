<div class="bios">

	<div class="pagetitle">
		<h1>Admin Panel</h1>
	</div>

	<div>
		<%= render "season" %>
		<%= render "match" %>
		<%= render "team" %>
		<%= render "user" %>
		<%= render "player" %>
	</div>

</div>