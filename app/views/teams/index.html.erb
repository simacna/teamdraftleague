<div class="teams">

	<div class="pagetitle">
		<h1>Teams</h1>
	</div>

	<!-- Iterates over each team and lists its name -->
	<% @teams.each do |team| %>
		<p><a href="/teams/<%= team.id %>"><%= team.name %></a></p>
	<% end %>

</div>